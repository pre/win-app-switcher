# Win App Switcher

macOS-style application switching for Windows. Every switcher found online
was bad in its own way, so this one exists.

How it differs from the built-in alt-tab:

- **WIN+TAB** shows one icon per running application — alt-tab shows every
  window of everything.
- **WIN+§** lists all windows of the active application — Windows has no
  equivalent.
